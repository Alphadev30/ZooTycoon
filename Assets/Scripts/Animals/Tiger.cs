using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tiger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Placed a Tiger");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
